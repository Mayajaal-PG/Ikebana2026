## Packages
react-qr-code | Generating QR codes for stall menus
framer-motion | For smooth page transitions, cart animations, and list staggering

## Notes
Using local storage for order history (no backend endpoint required for history)
Using UPI deep links for payments
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
